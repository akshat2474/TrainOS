name: train_os
description: A professional fitness tracking app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  pedometer: ^4.0.2
  shared_preferences: ^2.2.2
  fl_chart: ^0.65.0
  permission_handler: ^11.3.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
cupertino_icons: ^1.0.8
flutter_lints: ^5.0.0

